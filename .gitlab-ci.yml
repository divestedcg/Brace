before_script:
  - umask 0022
  - setfacl --restore=permissions.facl

build:
  image: fedora:latest
  script:
    - dnf update -y
    - dnf install -y acl rpm-build dpkg pacman
    - rpmbuild -ba brace.spec
    - dpkg-deb --build brace
    - makepkg
  artifacts:
    paths:
        - build/noarch/brace*.rpm
        - brace.deb
        - brace*.pkg.tar.*
